@{
  Layout = "_Layout";
}

@using AnimalShelter.Models;

<h1>Animals</h1>

@if (@Model.Count == 0)
{
  <h3>No furry friends yet</h3>
}

<ul>
@foreach(Animal animal in Model)
  {
    <li>@Html.ActionLink($"{animal.Description}", "Details", new { id = animal.Id})</li>
  }
</ul>

<p>@Html.ActionLink("Add an animal", "Create")</p>